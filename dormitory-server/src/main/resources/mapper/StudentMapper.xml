<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dormitory.dormitoryserver.mapper.StudentMapper">

    <select id="getByStudentNo" resultType="com.dormitory.dormitoryserver.entity.Student">
        SELECT * FROM student WHERE student_no = #{studentNo}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO student
        (student_no, password, name, gender, grade, phone, building_no, room_no, avatar, create_time, update_time)
        VALUES
            (#{studentNo}, #{password}, #{name}, #{gender}, #{grade}, #{phone}, #{buildingNo}, #{roomNo}, #{avatar}, #{createTime}, #{updateTime})
    </insert>

    <select id="getById" resultType="com.dormitory.dormitoryserver.entity.Student">
        SELECT * FROM student WHERE id = #{id}
    </select>

    <insert id="insertBatch">
        INSERT INTO student
        (student_no, password, name, gender, grade, phone, building_no, room_no, create_time, update_time)
        VALUES
        <foreach collection="students" item="st" separator=",">
            (#{st.studentNo}, #{st.password}, #{st.name}, #{st.gender}, #{st.grade}, #{st.phone}, #{st.buildingNo}, #{st.roomNo}, #{st.createTime}, #{st.updateTime})
        </foreach>
    </insert>

    <update id="update">
        UPDATE student
        <set>
            <if test="password != null">password = #{password},</if>
            <if test="name != null">name = #{name},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="grade != null">grade = #{grade},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>