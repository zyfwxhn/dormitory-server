<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dormitory.dormitoryserver.mapper.DeviceResourceMapper">

    <select id="pageQuery" resultType="com.dormitory.dormitoryserver.entity.DeviceResource">
        select * from device_resource
        <where>
            <if test="buildingNo != null and buildingNo != ''">
                and (building_no = #{buildingNo}
                     or building_no like concat(#{buildingNo}, '号楼%')
                     or building_no regexp concat('^', #{buildingNo}, '[^0-9]')
                     or (building_no like '一号楼%' and #{buildingNo} = '1')
                     or (building_no like '二号楼%' and #{buildingNo} = '2')
                     or (building_no like '三号楼%' and #{buildingNo} = '3')
                     or (building_no like '四号楼%' and #{buildingNo} = '4')
                     or (building_no like '五号楼%' and #{buildingNo} = '5')
                     or (building_no like '六号楼%' and #{buildingNo} = '6')
                     or (building_no like '七号楼%' and #{buildingNo} = '7')
                     or (building_no like '八号楼%' and #{buildingNo} = '8')
                     or (building_no like '九号楼%' and #{buildingNo} = '9')
                     or (building_no like '十号楼%' and #{buildingNo} = '10'))
            </if>
            <if test="deviceName != null and deviceName != ''">
                and device_name like concat('%', #{deviceName}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <update id="update">
        update device_resource
        <set>
            <if test="buildingNo != null and buildingNo != ''">building_no = #{buildingNo},</if>
            <if test="deviceName != null and deviceName != ''">device_name = #{deviceName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteBatch">
        delete from device_resource where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <insert id="insertBatch">
        INSERT INTO device_resource (building_no, device_name, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="d" separator=",">
            (#{d.buildingNo}, #{d.deviceName}, 1, NOW(), NOW())
        </foreach>
    </insert>

</mapper>